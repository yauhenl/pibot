allprojects {
    group = 'com.yauhenl'
    version = '1.0'
}

subprojects {
    buildscript {
        ext {
            springBootVersion = '1.3.6.RELEASE'
            kotlinVersion = '1.0.2'
            sshPluginVersion = '1.0.2'
        }
        repositories {
            mavenCentral()
            maven {
                url "https://plugins.gradle.org/m2/"
            }
        }
    }

    repositories {
        mavenCentral()
    }
}


task wrapper(type: Wrapper) {
    gradleVersion = '2.14.1'
}
